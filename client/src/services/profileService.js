// Profile service removed — stubs to avoid breaking imports
export const getProfile = async () => null;
export const updateProfile = async () => null;
export default { getProfile, updateProfile };
